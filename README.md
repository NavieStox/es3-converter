# The Development Environment for Transpile to ES3
It puts the source that javascript file written on ES5 or ES6 standard in `src`.
You run following command:
```
$ npm run bundle
```
This will generate a source of ES3 in `bin`.